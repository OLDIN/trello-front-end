export enum QueryKey {
  ME = 'me',
  USERS = 'users',
  GET_TASKS = 'get_tasks',
  GET_TASK_BY_ID = 'get_task_by_id',
  BOARDS = 'boards',
  TASK_LISTS = 'task_lists',
  CHECKLISTS = 'checklists',
  LABELS = 'labels',
}
